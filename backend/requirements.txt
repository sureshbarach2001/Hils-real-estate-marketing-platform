# ============================================================================
# Hils Marketing — Backend dependencies
# ============================================================================
# Install:
#     pip install -r requirements.txt
#
# Verify:
#     python -c "import fastapi, motor, jose, bcrypt; print('ok')"
# ============================================================================

# ---- Core API ----
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic[email]==2.10.4

# ---- Configuration ----
python-dotenv==1.0.1

# ---- MongoDB (async driver) ----
motor==3.6.0
pymongo==4.9.2

# ---- Auth (passwords + JWT) ----
# TODO(removed 2026-05-17): passlib[bcrypt]==1.7.4 dropped — unmaintained; spurious
# errors against bcrypt 4.x/5.x. Password hashing is bcrypt==4.2.1 in utils/auth.py.
bcrypt==4.2.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.20

# ---- Rate limiting (security.mdc requires it on /auth/*) ----
slowapi==0.1.9

# ---- Outbound HTTP ----
httpx==0.28.1

# ---- Payments ----
stripe==11.4.1

# ---- Email ----
aiosmtplib==3.0.2
jinja2==3.1.5

# ---- Utilities ----
python-dateutil==2.9.0.post0
geopy==2.4.1

# ---- Dropped: `emergentintegrations` ----
# Removed on 2026-05-17 after a CRITICAL malware advisory against the PyPI
# name `emergentintegrations` (CVSS 10.0, exfiltrates host info on install):
#   https://vulert.com/vuln-db/malicious-code-in-emergentintegrations--pypi-
# Nothing in this codebase imports from it. If you ever genuinely need an
# Emergent.sh integrations package in the future, install it explicitly
# with their private index URL — never re-add it to requirements.txt.

# ---- Dev / test ----
pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.4
